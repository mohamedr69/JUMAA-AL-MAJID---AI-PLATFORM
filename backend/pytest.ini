[pytest]
# Only tests/ holds tests. Without this pytest's root is backend/ and it walks
# the whole company library looking for test files -- 160-odd datasheet PDFs
# in the manufacturers' own folder names -- and on Windows falls over the
# first path past 260 characters ("The system cannot find the path
# specified") before a single test runs.
testpaths = tests
