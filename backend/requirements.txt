fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyjwt==2.10.1
python-multipart==0.0.20
pytest==8.3.4
httpx==0.28.1
pymupdf==1.28.2
pytesseract==0.3.13
numpy==2.5.3
Pillow==12.3.0
alembic==1.19.2
openpyxl==3.1.5
# The company's older compliance statements are .xls (app/compliance).
xlrd==2.0.1
# Claude, for the interpretation tasks the deterministic extractors hand off
# (app/ai). Imported only when AI_ENABLED=true.
anthropic==1.5.0
openai==3.13.0
# Drives Word to convert the warranty letter to PDF, so the package carries
# the company letterhead rather than a redrawn page. Windows only, and the
# import is optional: without it the warranty falls back to a drawn page.
pywin32==312; sys_platform == "win32"

# Reads a DXF: the floor-wise device count is arithmetic over the block
# references a drawing holds (app.services.floor_devices).
ezdxf==1.4.4
