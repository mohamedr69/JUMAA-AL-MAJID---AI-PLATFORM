"""Compliance statements: read the specification, reuse the company's past
answers, ask a model only about what neither settles, write the workbook.

- `spec_text`   the clauses of a specification, and who it was written for
- `verify`      whether a specification is this project's, for this system
- `statements`  reading a compliance statement workbook (past or submitted)
- `references`  the index of past statements in the project archive
- `matcher`     finding the past answer to a clause
- `assist`      the batched model calls, cached and budgeted
- `service`     prepare and check, end to end
- `writer`      the Excel workbook
"""
